# FastAPI dummy endpoint demo

Install dependencies:

```bash
pip install -r requirements.txt
```

Run server:

```bash
python -m uvicorn main:app --reload --port 8000
```

Run client (in a separate terminal):

```bash
python client.py
```
